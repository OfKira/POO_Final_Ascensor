# Proyecto Final POO – Sistema de Ascensor 🛗

Estructura lista con clases vacías para que agregues el código Java.  
Basado en el diseño UML del problema del elevador.

## 📁 Estructura
- `src/com/example/elevator/`: clases Java vacías (Main, Elevator, etc.)
- `docs/`: documentación UML
- `out/`: salida de compilación (vacía)

## 🚀 Uso
1. Agrega el código a cada clase.
2. Compila con:
   ```bash
   javac -d out $(find src -name "*.java")
   java -cp out com.example.elevator.Main
   ```
